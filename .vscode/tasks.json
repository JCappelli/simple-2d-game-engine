{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Windows Build with gcc",
			"command": "./windows_build.bat",
			"args": [
				"${workspaceFolder}"
			],
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "compiler: gcc from user PATH"
		}
	]
}